// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/team11/app/ent/booking"
	"github.com/team11/app/ent/cliententity"
	"github.com/team11/app/ent/servicepoint"
	"github.com/team11/app/ent/user"
)

// Booking is the model entity for the Booking schema.
type Booking struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// BOOKINGDATE holds the value of the "BOOKING_DATE" field.
	BOOKINGDATE time.Time `json:"BOOKING_DATE,omitempty"`
	// TIMELEFT holds the value of the "TIME_LEFT" field.
	TIMELEFT time.Time `json:"TIME_LEFT,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BookingQuery when eager-loading is set.
	Edges           BookingEdges `json:"edges"`
	CLIENT_ID       *int
	SERVICEPOINT_ID *int
	USER_ID         *int
}

// BookingEdges holds the relations/edges for other nodes in the graph.
type BookingEdges struct {
	// Usedby holds the value of the usedby edge.
	Usedby *User
	// Getservice holds the value of the getservice edge.
	Getservice *ServicePoint
	// Using holds the value of the using edge.
	Using *ClientEntity
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// UsedbyOrErr returns the Usedby value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BookingEdges) UsedbyOrErr() (*User, error) {
	if e.loadedTypes[0] {
		if e.Usedby == nil {
			// The edge usedby was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.Usedby, nil
	}
	return nil, &NotLoadedError{edge: "usedby"}
}

// GetserviceOrErr returns the Getservice value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BookingEdges) GetserviceOrErr() (*ServicePoint, error) {
	if e.loadedTypes[1] {
		if e.Getservice == nil {
			// The edge getservice was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: servicepoint.Label}
		}
		return e.Getservice, nil
	}
	return nil, &NotLoadedError{edge: "getservice"}
}

// UsingOrErr returns the Using value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BookingEdges) UsingOrErr() (*ClientEntity, error) {
	if e.loadedTypes[2] {
		if e.Using == nil {
			// The edge using was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: cliententity.Label}
		}
		return e.Using, nil
	}
	return nil, &NotLoadedError{edge: "using"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Booking) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{}, // id
		&sql.NullTime{},  // BOOKING_DATE
		&sql.NullTime{},  // TIME_LEFT
	}
}

// fkValues returns the types for scanning foreign-keys values from sql.Rows.
func (*Booking) fkValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{}, // CLIENT_ID
		&sql.NullInt64{}, // SERVICEPOINT_ID
		&sql.NullInt64{}, // USER_ID
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Booking fields.
func (b *Booking) assignValues(values ...interface{}) error {
	if m, n := len(values), len(booking.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	b.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullTime); !ok {
		return fmt.Errorf("unexpected type %T for field BOOKING_DATE", values[0])
	} else if value.Valid {
		b.BOOKINGDATE = value.Time
	}
	if value, ok := values[1].(*sql.NullTime); !ok {
		return fmt.Errorf("unexpected type %T for field TIME_LEFT", values[1])
	} else if value.Valid {
		b.TIMELEFT = value.Time
	}
	values = values[2:]
	if len(values) == len(booking.ForeignKeys) {
		if value, ok := values[0].(*sql.NullInt64); !ok {
			return fmt.Errorf("unexpected type %T for edge-field CLIENT_ID", value)
		} else if value.Valid {
			b.CLIENT_ID = new(int)
			*b.CLIENT_ID = int(value.Int64)
		}
		if value, ok := values[1].(*sql.NullInt64); !ok {
			return fmt.Errorf("unexpected type %T for edge-field SERVICEPOINT_ID", value)
		} else if value.Valid {
			b.SERVICEPOINT_ID = new(int)
			*b.SERVICEPOINT_ID = int(value.Int64)
		}
		if value, ok := values[2].(*sql.NullInt64); !ok {
			return fmt.Errorf("unexpected type %T for edge-field USER_ID", value)
		} else if value.Valid {
			b.USER_ID = new(int)
			*b.USER_ID = int(value.Int64)
		}
	}
	return nil
}

// QueryUsedby queries the usedby edge of the Booking.
func (b *Booking) QueryUsedby() *UserQuery {
	return (&BookingClient{config: b.config}).QueryUsedby(b)
}

// QueryGetservice queries the getservice edge of the Booking.
func (b *Booking) QueryGetservice() *ServicePointQuery {
	return (&BookingClient{config: b.config}).QueryGetservice(b)
}

// QueryUsing queries the using edge of the Booking.
func (b *Booking) QueryUsing() *ClientEntityQuery {
	return (&BookingClient{config: b.config}).QueryUsing(b)
}

// Update returns a builder for updating this Booking.
// Note that, you need to call Booking.Unwrap() before calling this method, if this Booking
// was returned from a transaction, and the transaction was committed or rolled back.
func (b *Booking) Update() *BookingUpdateOne {
	return (&BookingClient{config: b.config}).UpdateOne(b)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (b *Booking) Unwrap() *Booking {
	tx, ok := b.config.driver.(*txDriver)
	if !ok {
		panic("ent: Booking is not a transactional entity")
	}
	b.config.driver = tx.drv
	return b
}

// String implements the fmt.Stringer.
func (b *Booking) String() string {
	var builder strings.Builder
	builder.WriteString("Booking(")
	builder.WriteString(fmt.Sprintf("id=%v", b.ID))
	builder.WriteString(", BOOKING_DATE=")
	builder.WriteString(b.BOOKINGDATE.Format(time.ANSIC))
	builder.WriteString(", TIME_LEFT=")
	builder.WriteString(b.TIMELEFT.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Bookings is a parsable slice of Booking.
type Bookings []*Booking

func (b Bookings) config(cfg config) {
	for _i := range b {
		b[_i].config = cfg
	}
}
